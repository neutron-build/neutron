# Marketing Reference

Purpose: static-first marketing site patterns with content collections + MDX.

Run:

```bash
pnpm --dir examples/marketing-reference install
pnpm --dir examples/marketing-reference run dev
```
