/target
.pixi/
*.bak
*.backup
*.new
*.rs2
*mod_new.rs

# Scratch single-file test binaries compiled at the crate root (rustc foo.rs -o foo)
/test_*
/format_test
!/tests/

# Scratch repro SQL (padded-row workloads etc.) — never commit
ins*.sql
bench-estimates.json
