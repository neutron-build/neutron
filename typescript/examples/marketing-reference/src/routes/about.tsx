export const config = { mode: "static" };

export default function About() {
  return (
    <section>
      <h1>About This Reference</h1>
      <p>
        Keep marketing routes static whenever possible. Add islands only for focused interactions.
      </p>
    </section>
  );
}
