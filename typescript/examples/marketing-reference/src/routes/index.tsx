export const config = { mode: "static" };

export default function Home() {
  return (
    <section>
      <h1>Ship Fast Marketing Pages</h1>
      <p>
        This reference uses static routes and content collections for zero-JS page delivery.
      </p>
    </section>
  );
}
