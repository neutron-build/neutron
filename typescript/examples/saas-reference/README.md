# SaaS Reference

Purpose: app-route patterns (loaders/actions/forms/cache) without extra backend packages.

Run:

```bash
pnpm --dir examples/saas-reference install
pnpm --dir examples/saas-reference run dev
```
