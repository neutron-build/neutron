node_modules/
dist/
.turbo/
*.log
.DS_Store
benchmarks/results/latest.json
benchmarks/results/run-*.json
apps/playground/test-results/
apps/playground/playwright-report/

# Generated by scripts/init-smoke.mjs: it scaffolds every template through the
# real CLI into examples/ (a workspace glob, so the generated app can resolve
# @neutron-build/* the way a user's would). They are rebuilt on every run and
# were committed once by accident -- 5 apps, ~4,900 lines of generated code.
examples/init-smoke-*/
