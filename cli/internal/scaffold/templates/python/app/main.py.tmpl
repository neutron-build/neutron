"""{{.Name}} — built with Neutron."""

from neutron import App, Router, default_stack

# FRAMEWORK_CONTRACT §5 middleware order. GET /health, /openapi.json and /docs
# are built in.
app = App(title="{{.Name}}", version="0.1.0", middleware=default_stack())
router = Router()


@router.get("/api/hello")
async def hello() -> dict:
    return {"message": "Hello from {{.Name}}!"}


app.include_router(router)
