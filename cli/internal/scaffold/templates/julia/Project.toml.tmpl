[deps]
{{.SDK.Name}} = "{{.SDK.UUID}}"

# NeutronJulia is not in the General registry; it is the `{{.SDK.Subdir}}/`
# directory of the Neutron repository. `[sources]` needs Julia 1.11+.
[sources]
{{.SDK.Name}} = {url = "{{.SDK.Source}}", subdir = "{{.SDK.Subdir}}"}
